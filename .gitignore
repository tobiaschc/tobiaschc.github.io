# Personal Worker config — never commit (contains your system prompt)
chat-worker/config.js

# Wrangler build artifacts
.wrangler/
chat-worker/.wrangler/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# CV source files (LaTeX, drafts) — keep private, but publish the PDF linked from the site
cv/*
!cv/tobias_chavarria_c_CV.pdf
